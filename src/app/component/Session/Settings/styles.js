const styles = ({ palette }) => ({
  root: {
    width: 300,
    padding: "15px 0"
  },
  form: {
    display: "flex",
    flexDirection: "column",
    "& > *": {
      margin: "10px 0"
    }
  }
});

export default styles;
